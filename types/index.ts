export interface CustomCardProps {
  backgroundColor?: string;
  value?: number;
  title: string;
  isDisabled?: boolean;
  isActive?: boolean;
};

export interface CustomButtonProps {
  title:string;
  handleClick?:
}
